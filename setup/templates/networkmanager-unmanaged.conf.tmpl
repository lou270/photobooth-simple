# GENERATED by setup/apply-wifi.sh
#
# NetworkManager must keep its hands off the access point interface: it would
# otherwise take the address back off it and try to join a network as a client.
[keyfile]
unmanaged-devices=interface-name:${WIFI_INTERFACE}
