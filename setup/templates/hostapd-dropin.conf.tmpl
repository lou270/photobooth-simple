# GENERATED by setup/apply-wifi.sh
#
# hostapd started before the interface had its static address would bind to
# nothing and fail; this orders it after the unit that assigns the address.
[Unit]
After=photobooth-ap-network.service
Requires=photobooth-ap-network.service

[Service]
ExecStartPre=/usr/sbin/rfkill unblock wifi
