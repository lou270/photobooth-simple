[Global]
# Interface language: the booth's screen, and the admin web pages. Guests on
# their own phone (gallery, send-a-photo page) instead get whatever language
# their phone asks for, independently of this setting. Available: en, fr
LANGUAGE = en

# If set to True, the window fill be fullscren
FULLSCREEN = False

# Size of the booth window, in pixels. This is also the mode fullscreen runs at,
# so it has to match the panel: 1024 x 600 for the Ingcool 7" touchscreen,
# 1920 x 1080 for a full HD monitor. The interface scales to whatever is set
# here, portrait included. Takes effect on the next start.
WINDOW_WIDTH = 1024
WINDOW_HEIGHT = 600

# Quarter turn applied to the whole interface, for a panel mounted on its side:
# 0, 90, 180 or 270. Leave WINDOW_WIDTH and WINDOW_HEIGHT at the panel's own
# mode, they are not swapped: a 1920 x 1080 screen standing upright is
# 1920 / 1080 / 90. The booth rotates its own display and its own touch input,
# so nothing has to be configured in the desktop or the touchscreen driver.
# Leave this at 0 when developing on Windows: Kivy sizes its viewport from the
# rotated size there and clips the interface to a corner of the window. The
# booth's own Linux host renders it correctly.
ROTATION = 0

# If set to True, the review screen shows a SHARE button whose QR code leads the
# guest to their own photo. Leave it False when guests are not meant to reach
# the booth during an event: the booth then shows no QR code pointing at a
# server they cannot use.
SHARE = False

# If set to True, RingLed will be enabled (requires spidev hardware)
RINGLED = True

# Number of LEDs on the ring, so the animations go all the way round it
RINGLED_PIXELS = 12

# Password protecting the admin pages: config editor, template editor, log viewer
# and deletion of every saved session. Use None to keep admin access disabled.
# Must be at least 10 characters and not a well-known value, otherwise admin
# access stays disabled and a message is written to the log.
ADMIN_PASSWORD = None

[Event]
# What makes the booth this evening's booth. All of it can be set from the admin
# page, which is also where the photo behind the welcome screen is uploaded.
# Applied on the next start of the booth.

# The big words on the welcome screen, a whole sentence if you like: it breaks
# between words onto even lines. Empty keeps the booth's own title.
WELCOME_TITLE =

# A line under the title, such as the names and the date. Empty shows none.
WELCOME_SUBTITLE =

# The lettering of the title and subtitle: elegant, script, modern or playful.
WELCOME_FONT = elegant

# Printed wherever a template text says {event}
EVENT_NAME =

# How {date} prints on a template, as a strftime pattern: %d day, %m month,
# %Y year. Templates can also print {time}, as hours:minutes.
DATE_FORMAT = %d/%m/%Y

[Slideshow]
# Show the evening's photos on the welcome screen once nobody has touched it for
# SLIDESHOW_IDLE_SECONDS. A touch brings the welcome screen back.
SLIDESHOW = False
SLIDESHOW_IDLE_SECONDS = 60
SLIDESHOW_PHOTO_SECONDS = 6

[Timing]
# How long the booth waits for a guest, in seconds. A screen nobody answers goes
# back to the welcome screen on its own, so the next guest never finds the
# previous one's session.

# The photo just taken is kept on its own after this long, unless the guest asks
# to retake it
AUTO_KEEP_SECONDS = 6

# Back to the welcome screen when nobody presses the button for the next shot
COUNTDOWN_IDLE_SECONDS = 30

# ...when nobody picks a format
SELECT_FORMAT_IDLE_SECONDS = 30

# ...when nobody prints or leaves the final photo. It is not printed.
REVIEW_IDLE_SECONDS = 60

# ...when nobody picks one of the photos phones sent
PHONE_PHOTOS_IDLE_SECONDS = 90

# ...from an error a guest can continue past (a failed print, say). An error
# that needs an operator stays on screen.
ERROR_IDLE_SECONDS = 90

# The QR codes close on their own after this long
QR_CODES_IDLE_SECONDS = 45

# Shortest time the printing screen stays, so the guest reads where to collect
# the photo. A touch skips it.
PRINT_SCREEN_MIN_SECONDS = 6

# Safety nets, not waits: a capture taking longer than this resets the camera,
# and a print job taking longer than this per sheet is reported as failed.
# Raise them only for a camera or a printer known to be slower.
CAPTURE_TIMEOUT_SECONDS = 10
PRINT_SHEET_TIMEOUT_SECONDS = 180

[Web]
# Port used by the gallery and admin web server
WEB_PORT = 5000

# Address the web server binds to.
# 0.0.0.0    reachable from every network the booth is attached to
# 127.0.0.1  local only, nothing reachable from the network
# 192.168.x.y pinned to a single interface
WEB_HOST = 0.0.0.0

[WiFi]
# The network guests join to reach the booth: a travel router next to it, or
# the venue's WiFi. The booth does not create or configure this network, it
# only puts its credentials in a QR code, shown before the one carrying the
# address, so a phone joins without anyone typing. Leave WIFI_SSID empty to
# show the address code alone, for guests who are on that network already.
WIFI_SSID =

# Empty for an open network
WIFI_PASSWORD =

# True only if the network does not broadcast its name
WIFI_HIDDEN = False

[Remote]
# Turn every guest phone into a camera for the booth: the welcome screen shows a
# QR code, the phone opens a page, takes a photo and sends it here. The photos
# wait in a queue the booth displays, and a guest prints the one they want.
REMOTE_CAPTURE = False

# Address every QR code points at, this one and the sharing one. None derives
# it from the interface the booth uses to reach its network, which is right for
# a booth with a single connection. Set it by hand when the booth has several
# (ethernet and WiFi), sits behind a port forward, or has a fixed name, and give
# the whole address, port included: 192.168.8.20:5000
REMOTE_URL = None

# Largest upload accepted, in megabytes
REMOTE_MAX_UPLOAD_MB = 12

# Longest side kept when an incoming photo is re-encoded. Above the print
# resolution there is nothing to gain and a lot of disk to lose.
REMOTE_MAX_IMAGE_PIXELS = 2400

# How many photos one phone may leave waiting, and how many the queue holds in
# total. Both are there so one guest cannot fill the booth on their own.
REMOTE_MAX_PER_SENDER = 20
REMOTE_MAX_PENDING = 200

# Seconds a phone must wait between two sends
REMOTE_MIN_UPLOAD_INTERVAL = 3

[Log]
# Keep logs bounded for long unattended runs
LOG_RETENTION_DAYS = 14
LOG_MAX_FILES = 40

[Capture]
# Capture backend: auto, gphoto2, picamera2, opencv, fake
# auto  = probe every backend and pick the best combination (slower startup)
# fake  = synthetic camera, runs the app with no hardware at all (development/tests)
# Naming the backend explicitly makes startup faster and predictable on a known machine.
CAMERA = auto

# Countdown before the photo is taken
COUNTDOWN = 5

# If using a different camera for preview and capture,
# run tools/calibration_zoom.py to find the correct tuple to apply (zoom, offset_x, offset_y), None to disable
CALIBRATION = None

# If set to True, photo filters selection will be enabled on confirm screen
FILTERS = False

# Recompute blurred preview borders only every N frames to reduce CPU usage.
# 1 = recompute every frame, 3 = reuse the same blur for 2 frames before refreshing.
PREVIEW_BLUR_REFRESH_FRAMES = 3

# Add blurry borders to camera preview, captured images, and collages.
BLUR_CAMERA = True
BLUR_IMAGES = False
BLUR_COLLAGE = False

[Storage]
# Directory in which the photos and collages are stored
DCIM_DIRECTORY = ./DCIM

# Stop new sessions before storage failures become likely
DISK_MIN_FREE_GB = 2.0
DISK_MAX_USED_PERCENT = 90.0

[Print]
# The printer's name in CUPS (None to disable)
PRINTER = DS620

# Maximum number of prints allowed (None for unlimited)
MAX_PRINTS = None

# Maximum sheets a guest can ask for in one print job (1 hides the selector).
# A template that duplicates its collage puts two photos on each sheet, so the
# booth offers up to twice this many finished photos.
MAX_COPIES = 3

# Resolution the collage is assembled at: 300 or 600 dpi. Templates are drawn at
# 300 dpi, what the printer prints, so 600 does not change the print: it makes
# the saved collage sharper on a screen or a phone, at the cost of files about
# four times larger and a slower collage on the Raspberry Pi.
COLLAGE_DPI = 300

# Seconds to wait for printer recovery before continuing without printing
PRINTER_WAIT_TIMEOUT = 45

[USB]
# Copy saved sessions to removable USB media when inserted
USB_EXPORT = True
USB_MIN_FREE_GB = 1.0

[DSLR_Liveview]
# Parameters applied to the DSLR during liveview (preview).
# Leave empty or None to leave the camera setting unchanged.
SHUTTERSPEED = 1/30
APERTURE = 8
FOCUSMODE =
ISO = 1600

[DSLR_Capture]
# Parameters applied to the DSLR right before each photo capture.
# Leave empty or None to leave the setting unchanged.
SHUTTERSPEED = 1/125
APERTURE = 8
FOCUSMODE =
ISO = 400
